[egg_info]
tag_build = .dev
tag_svn_revision = 1

[aliases]
release = egg_info -RDb ''
bdist_egg = build_docs bdist_egg

[nosetests]
verbosity = 0
with-doctest = 1
detailed-errors = 1
with-coverage = 1
tests = enthought/envisage/developer/code_browser/tests,enthought/envisage/ui/action/tests
#
# The following directories are included in the tests option above, when
# the line starts with '# test '.  To make changes in the tests option above
# run the script update_setup_cfg.py.
#
# skip enthought/envisage/developer/charm/tests (empty dir)
# test enthought/envisage/developer/code_browser/tests
# test enthought/envisage/ui/action/tests
