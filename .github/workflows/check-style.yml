name: Run style checks

on: [pull_request, workflow_dispatch]

jobs:
  style:
    runs-on: 'ubuntu-latest'

    steps:
    - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
    - name: Set up uv
      uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
      with:
        python-version: '3.14'
        enable-cache: true
        cache-dependency-glob: 'pyproject.toml'
    # The style tools don't need Envisage itself, so the project isn't
    # installed into the environment they run in.
    - run: |
        uv run --only-group style -- python -m black --check --diff .
        uv run --only-group style -- python -m isort --check --diff .
        uv run --only-group style -- python -m flake8 .
